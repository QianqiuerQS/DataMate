# Independent service production dependencies verified in the 910b
# data-synthesis-service container (my_npu_env:v1, Python 3.10.16).
# Do not put these heavy model dependencies into operator_src/requirements.txt.
fastapi==0.136.3
uvicorn==0.49.0
pydantic==2.10.6
Jinja2==3.1.5
requests==2.32.5
torch==2.1.0
torch-npu==2.1.0
transformers==4.52.4
tokenizers==0.21.4
sentencepiece==0.2.1
einops==0.8.2
numpy==1.26.4
safetensors==0.7.0
modelscope==1.34.0
pandas==2.2.3
matplotlib==3.10.0

# Optional backend for vLLM mode. The default verified service path uses the
# Transformers backend; if DATA_SYNTHESIS_BACKEND=vllm is enabled, use:
# vllm==0.13.0+empty
# vllm-ascend==0.13.0
